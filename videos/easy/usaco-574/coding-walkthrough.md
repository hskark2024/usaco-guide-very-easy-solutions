# C++ coding walkthrough

1. Open feast.in when it exists, otherwise use standard input.
2. Allocate before and after byte arrays.
3. Build the before closure and seed all halved states.
4. Build the after closure with no second drink.
5. Scan both layers from T downward.
